spring:
  application:
    name: blog-api
  jackson:
    time-zone: Asia/Seoul
  profiles:
    default: local
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        jdbc:
          time_zone: UTC
        format_sql: true

blog:
  api:
    base-path: /api
